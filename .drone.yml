---
kind: pipeline
type: kubernetes
name: default

steps:
- name: build gatsby
  image: node:14.15.3-alpine3.12
  commands:
  - cd dev.simonemessaggi.it && node -v
  - ls -la
  - npm ci
  - mv static/robots.prod static/robots.txt
  - npm run build
